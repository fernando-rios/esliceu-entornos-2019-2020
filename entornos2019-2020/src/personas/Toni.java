public class Toni extends Persona {

	public Toni(String nombre) {
		super("Toni");
	}

	public String llamame(String frase) {
  String nuevafrase = "";
    for (int i = 0; i < frase.length(); i++){
      switch (frase.charAt(i)){
        if (frase.charAt(i).equals('a') || frase.charAt(i).equals('e') || frase.charAt(i).equals('i') || frase.charAt(i).equals('c')))
          nuevafrase += h;
        else
          nuevafrase += frase.charAt(i);
       
      }
    }
    nuevafrase += ' siempre, bueno a veces.'
		return nuevafrase;
	}
}
